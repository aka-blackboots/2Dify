class twoDify{
    constructor(scene, camera, container)
    {
        this.scene = scene;
        this.camera = camera;
        this.container = container;
    }

    addMarker(){
        console.log("Adding marker");
    }

    createOrthoView(){

    }
}

export { twoDify };
